# overlay לפיתוח מקומי בלבד: חושף את Postgres ו-Redis ל-127.0.0.1
# כדי שאפשר יהיה להריץ את האפליקציה מחוץ ל-Docker מול ה-DB בקונטיינר.
# לעולם לא בפרודקשן.
services:
  postgres:
    ports:
      - "127.0.0.1:5433:5432"
  redis:
    ports:
      - "127.0.0.1:6380:6379"
