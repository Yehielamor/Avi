# פיתוח מקומי. VITE_API_URL ריק = /v1 באותו מקור, ו-vite מעביר ל-API (vite.config.ts).
# בלי VITE_TENANT הבקשה מגיעה מ-localhost בלי תת-דומיין, והשרת דוחה אותה (400).
# ציבורי — אסור סוד.
VITE_API_URL=
VITE_TENANT=ac-maintenance
